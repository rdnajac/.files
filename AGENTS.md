# Global instructions

- Use `semble search` before grep+read! Navigate directly to the returned file
  and line — do not re-search or grep for the same content.

## Code Search

Always perform code searches before responding when appropriate.

## Read the docs!

### prefer local docs

neovim: `$VIMRUNTIME`

### some useful online docs

chezmoi: `https://www.chezmoi.io/{reference,user-guide}/`
micromamba: `https://mamba.readthedocs.io/en/latest/user_guide/micromamba.html`

### don't fforget to read the man pages!

ghostty: `man ghostty`
