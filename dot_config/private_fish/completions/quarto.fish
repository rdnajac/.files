# fish completions for `quarto`
# Options:
#
#   -h, --help     - Show this help.
#   -V, --version  - Show the version number for this program.
#
# Commands:
#
#   render     [input] [args...]     - Render files or projects to various document types.
#   preview    [file] [args...]      - Render and preview a document or website project.
#   serve      [input]               - Serve a Shiny interactive document.
#   create     [type] [commands...]  - Create a Quarto project or extension
#   use        <type> [target]       - Automate document or project setup tasks.
#   add        <extension>           - Add an extension to this folder or project
#   update     [target...]           - Updates an extension or global dependency (TinyTeX, Chromium, Chrome Headless Shell, VeraPDF).
#   remove     [target...]           - Removes an extension.
#   convert    <input>               - Convert documents to alternate representations.
#   pandoc     [args...]             - Run the version of Pandoc embedded within Quarto.
#   typst      [args...]             - Run the version of Typst embedded within Quarto.
#   run        [script] [args...]    - Run a TypeScript, R, Python, or Lua script.
#   list       <type>                - Lists an extension or global dependency.
#   install    [target...]           - Installs a global dependency (TinyTeX, Chromium, Chrome Headless Shell, VeraPDF).
#   uninstall  [tool]                - Uninstalls a global dependency (TinyTeX, Chromium, Chrome Headless Shell, VeraPDF).
#   tools                            - Display the status of Quarto installed dependencies
#   publish    [provider] [path]     - Publish a document or project to a provider.
#   check      [target]              - Verify correct functioning of Quarto installation.
#   call                             - Access functions of Quarto subsystems such as its rendering engines.
#   help       [command]             - Show this help or the help of a sub-command.
