{{- $a := splitn "/" 2 . -}}
{{- $user := $a._0 -}}
{{- $repo := $a._1 -}}
{{- $dir := "chezmoiexternal" -}}
{{- if eq $user "tpope" }}
  {{- $dir = $user -}}
{{- end }}
{{ printf ".vim/pack/%s/start/%s" $dir $repo }}:
  type: git-repo
  url: https://github.com/{{ . }}.git
  refreshPeriod: 168h
